{
  "name": "ai-visual-studio",
  "version": "1.0.0",
  "description": "Upload model + product images and a prompt — get an AI-generated result via n8n",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
